#pragma once

#include "effects.hpp"

// Function to generate a test image with random pixels
void generate_test_image(ImageProcessor& proc, int size);

// Function to run all benchmarks
void run_benchmarks();
